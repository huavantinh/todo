const button = document.querySelector("#button-submit");

button.addEventListener("click", (event) => {
    var content = document.getElementById('input-sub').value;
    console.log(content);
});

window.addEventListener("load", () => {

});